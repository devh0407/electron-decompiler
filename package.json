{
  "name": "electron-decompiler",
  "version": "0.2.0",
  "private": true,
  "description": "Inspect, extract, analyze and safely preview Electron app.asar renderer pages.",
  "main": "src/main/main.cjs",
  "scripts": {
    "start": "electron .",
    "test": "node --test tests/*.test.cjs",
    "check": "node scripts/check-syntax.cjs"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "@electron/asar": "4.2.1"
  },
  "devDependencies": {
    "electron": "43.2.0"
  }
}
